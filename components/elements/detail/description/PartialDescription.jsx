import React from 'react';

const PartialDescription = (product) => {

    return (
    <div className="ps-document">
        {product?.product?.product?.product_desc}
    </div>
)};

export default PartialDescription;
